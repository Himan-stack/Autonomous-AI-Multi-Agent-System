GEMINI_API_KEY=your_gemini_api_key

GROQ_API_KEY=your_groq_api_key

GEMINI_MODEL=gemini-2.5-flash

GROQ_MODEL=llama-3.3-70b-versatile

# Comma-separated. In production this is the public HTTPS origin;
# CORS mostly won't even be hit since Caddy makes frontend+backend
# same-origin, but this keeps direct-origin testing correct too.
ALLOWED_ORIGINS=https://3-108-215-197.sslip.io,http://localhost:5173,http://localhost:5174